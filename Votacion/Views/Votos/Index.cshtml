@model IEnumerable<SistemaElecciones.Models.Voto>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Candidato)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Usuario)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Candidato.Apellidos)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Usuario.Apellidos)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.VotoID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.VotoID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.VotoID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
